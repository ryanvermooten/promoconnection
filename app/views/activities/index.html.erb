<h1> User's activities </h1>

<% @activities.each do |activity| %>
	<div class="activity">
	<%= link_to activity.owner.name, activity.owner if activity.owner %>
	added comment to <%= link_to activity.trackable.template, activity.trackable %> 
	</div>
	<% end %>
